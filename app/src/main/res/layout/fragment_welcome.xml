<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".ui.WelcomeFragment"
    android:background="@drawable/juego_inicio">

    <ImageView
        android:id="@+id/imageView2"
        android:contentDescription="@string/game_background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:id="@+id/play_button"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_width="90dp"
        android:layout_height="95dp"
        android:fontFamily="@font/pokemon_solid"
        android:text="@string/play_button"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:translationX="97dp"
        android:translationY="470dp"
        app:cornerRadius="100dp" />

    <Button
        android:id="@+id/help_button"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/pokemon_solid"
        android:text="@string/help_button"
        android:textColor="#F3EF32"
        android:textSize="16sp" />

    <Button
        android:id="@+id/stats_button"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:fontFamily="@font/pokemon_solid"
        android:text="@string/stats_button"
        android:textColor="#F3EF32" />

</RelativeLayout>